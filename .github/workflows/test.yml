name: Validate Windows scripts

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run PowerShell 5.1 project checks
        shell: powershell
        run: .\tests\Test-Project.ps1
